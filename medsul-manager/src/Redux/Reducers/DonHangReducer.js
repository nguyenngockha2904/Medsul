let initialState = {
    listDonHang: [],
}

const qlDonHangReducer = (state = initialState, { type, payload }) => {
    switch (type) {

        default: {
            return state;
        }
    }
}
export default qlDonHangReducer;